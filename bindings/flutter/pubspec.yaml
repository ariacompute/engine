name: ariaengine
description: Flutter FFI binding for Aria Engine. Load an Aria bundle and run complete() via package:ariaengine.
version: 0.1.0
homepage: https://ariacompute.com
repository: https://github.com/ariacompute/engine
issue_tracker: https://github.com/ariacompute/engine/issues
environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.10.0"
dependencies:
  flutter:
    sdk: flutter
  ffi: ^2.1.0
dev_dependencies:
  test: ^1.25.0
