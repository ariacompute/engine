[package]
name = "aria-graph"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Aria zero-copy compute graph"

[dependencies]
aria-kernel = { workspace = true }

[dev-dependencies]
tempfile = { workspace = true }
